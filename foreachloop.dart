void main() {
  // List<String> names = ['Raj', 'Tharinda', 'Abeysena', 'Eppa'];

  // List<int> nums = [1, 2, 3, 4, 5];

  // int tot = 0;

  // names.forEach((name) {
  //   print(name);
  // });

  // nums.forEach((nums) => tot = tot + nums);

  // nums.forEach((num) {
  //   tot = tot + num;
  // });

  // print('Total is $tot');

  // double avg = tot / nums.length;
  // print('Average is $avg');

  List<String> names = ['Raj', 'Tharinda', 'Abeysena', 'Eppa'];

  for (String name in names) {
    print(name);
  }
}
